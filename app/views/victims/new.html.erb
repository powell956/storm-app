
<%= form_for @victim, url: {action: "create"}, html: {class: "nifty_form"} do |f| %>

  <%= f.text_field :name %>
  <%= f.number_field :age %>
  <%= f.collection_select :storm_id, Storm.all, :id, :name %>
  <%= f.submit "Create" %>
<% end %>
