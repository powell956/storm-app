
<%= form_for @storm, url: {action: "create"}, html: {class: "nifty_form"} do |f| %>

  <%= f.text_field :name %>
  <%= f.number_field :severity %>
  <%= collection_select "Victim", :name, Victim.all, :id, :name %>
  <%= f.submit "Create" %>
<% end %>
